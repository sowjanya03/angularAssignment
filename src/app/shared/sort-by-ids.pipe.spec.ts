import { SortByIdsPipe } from './sort-by-ids.pipe';

describe('SortByIdsPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByIdsPipe();
    expect(pipe).toBeTruthy();
  });
});
