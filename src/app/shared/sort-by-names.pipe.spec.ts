import { SortByNamesPipe } from './sort-by-names.pipe';

describe('SortByNamesPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByNamesPipe();
    expect(pipe).toBeTruthy();
  });
});
